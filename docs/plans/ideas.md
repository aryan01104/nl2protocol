
1. in some of our controls the code seems to do better in some cases? do we benefit from comparing our extracted spec to a control generated spec, to see which steps if at all we may have missed? or could that also introduce some noise?
2. I checked the opentrons api, and it seems to include many more functions that I see we include. are those functions syntactic sugar for our functions? can the functions we include, do all that their complete set of functions does?

