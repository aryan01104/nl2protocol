# Source — Simple transfer floor (hand-authored)

**Origin:** Hand-authored baseline. Not derived from a published lab protocol.
**Why hand-authored:** This is the floor case — the simplest possible OT-2 instruction. Published protocols never describe an isolated single transfer because no protocol IS just one transfer; it's a building block. Authoring directly is the realistic move: this represents what a user might type to test whether the tool runs at all, or to do a one-off small task.

The instruction below is representative of the simplest realistic user input — explicit, unambiguous, single operation.
